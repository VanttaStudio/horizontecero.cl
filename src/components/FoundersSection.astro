<div class="founders-section">
    <div class="section-head">
        <h2>Quienes Somos</h2>
        <p>Mentes y manos detrás de Horizonte Cero</p>
    </div>

    <div class="founders-grid">
        <div class="founder-card">
            <div class="card-top">
                <div class="avatar">CR</div>
                <div class="meta">
                    <h3>César Rosales</h3>
                    <span class="role">Agrónomo y Fundador</span>
                </div>
            </div>
            <p class="bio">Con más de 18 años de experiencia en gestión agrícola y una herencia familiar ligada a la apicultura.</p>
            <div class="quote">"El respeto por la tierra es la base de mi trabajo."</div>
        </div>

        <div class="founder-card">
            <div class="card-top">
                <div class="avatar">JG</div>
                <div class="meta">
                    <h3>Jorge González</h3>
                    <span class="role">Agrónomo y Fundador</span>
                </div>
            </div>
            <p class="bio">Experto en la industria vitivinícola y gestión de calidad. Aporta una visión estratégica en eco-sostenibilidad.</p>
            <div class="quote">"Pequeñas acciones construyen un futuro sostenible."</div>
        </div>
    </div>
</div>

<style>
    .founders-section { margin-bottom: 80px; width: 100%; }
    
    .section-head { text-align: center; margin-bottom: 40px; }
    .section-head h2 { font-size: 2.5rem; font-weight: 800; color: #1e293b; margin: 0; }
    :global(.dark) .section-head h2 { color: white; }
    .section-head p { color: #64748b; font-size: 1.1rem; }

    /* GRID DE 2 COLUMNAS */
    .founders-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 30px;
    }
    @media (min-width: 900px) {
        .founders-grid { grid-template-columns: 1fr 1fr; }
    }

    /* CARD DESIGN */
    .founder-card {
        background: rgba(255, 255, 255, 0.75);
        backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.6);
        border-radius: 24px; padding: 35px;
        display: flex; flex-direction: column;
        transition: transform 0.3s;
    }
    :global(.dark) .founder-card {
        background: rgba(30, 41, 59, 0.6);
        border-color: rgba(255,255,255,0.1);
    }
    .founder-card:hover { transform: translateY(-5px); border-color: var(--color-secondary, #4ade80); }

    .card-top { display: flex; align-items: center; gap: 20px; margin-bottom: 20px; }
    
    .avatar {
        width: 60px; height: 60px; background: #e0f2fe; color: #0284c7;
        border-radius: 50%; display: flex; align-items: center; justify-content: center;
        font-weight: 900; font-size: 1.2rem;
    }
    
    .meta h3 { margin: 0; font-size: 1.3rem; font-weight: 800; color: #1e293b; }
    :global(.dark) .meta h3 { color: white; }
    
    .role { color: var(--color-secondary, #4ade80); font-weight: bold; text-transform: uppercase; font-size: 0.8rem; }

    .bio { color: #475569; line-height: 1.6; margin-bottom: 20px; flex-grow: 1; }
    :global(.dark) .bio { color: #cbd5e1; }

    .quote {
        border-top: 1px solid rgba(0,0,0,0.1); padding-top: 15px;
        font-style: italic; color: #94a3b8; font-size: 0.95rem;
    }
</style>