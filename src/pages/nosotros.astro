---
import MainLayout from '../layouts/MainLayout.astro';
import DnaSection from '../components/DnaSection.astro';
import StatsSection from '../components/StatsSection.astro';
import FoundersSection from '../components/FoundersSection.astro';
import PartnersCarousel from '../components/PartnersCarousel.astro';
---

<MainLayout title="Horizonte Cero | Nosotros">

  <div id="fixed-bg"></div>

  <div class="page-container">

    <div class="hero-split-grid">
        <div class="hero-left">
            <h1 class="main-title">
                Nuestra <span class="highlight">Raíz</span>
            </h1>
            <span class="pill-tag">Horizonte O</span>
        </div>

        <div class="hero-right glass-box">
            <p class="story-content">
                El nombre <strong>Horizonte Cero</strong> nace de la clasificación del suelo, donde el <span class="highlight">'Horizonte O'</span> es la primera capa, rica en materia orgánica y cuna de la vida. Representa nuestro punto de partida para regenerar, reconectar y construir un futuro sostenible desde la base.
            </p>
        </div>
    </div>

    <DnaSection />
    <StatsSection />
    <FoundersSection />
    <PartnersCarousel />

  </div>
</MainLayout>

<style is:global>
    html:not(.dark) #site-header nav a { color: #1f2937 !important; }
    html:not(.dark) #header-logo { background-image: url('/images/logodia.png') !important; }
    html.dark #site-header nav a { color: white !important; }
    html.dark #header-logo { background-image: url('/images/logonoche.png') !important; }
</style>

<style>
  /* CONFIGURACIÓN DE PÁGINA */
  .page-container {
    max-width: 1200px;
    margin: 0 auto;
    /* PADDING GIGANTE PARA BAJAR TODO */
    padding-top: 240px; 
    padding-bottom: 100px;
    padding-left: 20px; padding-right: 20px;
    position: relative;
    z-index: 10;
  }

  /* FONDO FIJO */
  #fixed-bg {
    position: fixed; inset: 0; z-index: 0; pointer-events: none;
    background-size: cover; background-position: center; background-repeat: no-repeat;
    background-image: url('/images/dia.png');
  }
  :global(.dark) #fixed-bg { background-image: url('/images/noche.png'); }

  /* Capa de contraste */
  #fixed-bg::after {
    content: ''; position: absolute; inset: 0;
    background: rgba(255,255,255,0.4);
  }
  :global(.dark) #fixed-bg::after { background: rgba(0,0,0,0.6); }

  /* HERO GRID */
  .hero-split-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 40px; margin-bottom: 80px; align-items: center;
  }
  @media (min-width: 900px) {
      .hero-split-grid { grid-template-columns: 40% 60%; }
  }

  .main-title { font-size: 3.5rem; font-weight: 800; line-height: 1; color: #1e293b; margin-bottom: 1rem; }
  :global(.dark) .main-title { color: white; }
  .highlight { color: var(--color-secondary, #4ade80); }

  .pill-tag {
      display: inline-block; background: var(--color-secondary, #4ade80); color: white;
      font-weight: bold; padding: 6px 20px; border-radius: 50px; text-transform: uppercase;
  }

  .glass-box {
      background: rgba(255, 255, 255, 0.7);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.5);
      border-radius: 20px; padding: 30px;
  }
  :global(.dark) .glass-box {
      background: rgba(30, 41, 59, 0.6); border-color: rgba(255,255,255,0.1);
      color: white;
  }
  .story-content { font-size: 1.2rem; line-height: 1.6; }

</style>